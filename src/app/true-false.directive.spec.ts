import { TrueFalseDirective } from './true-false.directive';

describe('TrueFalseDirective', () => {
  it('should create an instance', () => {
    const directive = new TrueFalseDirective();
    expect(directive).toBeTruthy();
  });
});
